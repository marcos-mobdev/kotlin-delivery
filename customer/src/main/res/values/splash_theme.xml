<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Splash" parent="Theme.SplashScreen">
        <!-- Customize your light theme here. -->
        <item name="windowSplashScreenBackground">@color/primary</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_rounded</item>
        <item name="android:windowSplashScreenBrandingImage" tools:ignore="NewApi"> @drawable/splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.KotlinDelivery</item>

    </style>

</resources>